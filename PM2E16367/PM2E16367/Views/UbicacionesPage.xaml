<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PM2E16367.Views.UbicacionesPage">


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Atras"
                     Clicked="toolbar01_Clicked"
                     x:Name="toolbar01"/>
    </ContentPage.ToolbarItems>
    
    

    <ListView x:Name="lsubicaciones" ItemTapped="lsubicaciones_ItemTapped">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        
                        <Label Text="{Binding descripcionc}" Grid.Row="0" Grid.Column="0" TextColor="Black" />
                    </Grid>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

    
    

</ContentPage>